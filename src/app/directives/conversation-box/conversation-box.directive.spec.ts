import {ConversationBoxDirective} from './conversation-box.directive';

describe('ConversationBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new ConversationBoxDirective();
    expect(directive).toBeTruthy();
  });
});
